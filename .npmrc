# Use pnpm's default (isolated, symlinked) node_modules layout — matches every other
# pnpm project on this machine (incl. Electron apps like RedX). Hoisted mode was tried
# and reliably hung during linking on this workspace; don't reintroduce it.
